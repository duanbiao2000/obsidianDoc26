# 部门化房间逻辑

PARA 模型将“项目/领域/资源/归档”置于同一抽象层级，导致检索时的逻辑噪音。我曾将健康数据与归档项目混杂，检索健康信息时需要跨过大量无关节点。解决方案是按认知功能模块切分物理文件夹（如 `01_认知架构` / `02_口语效能` / `03_工程封装`），每个文件夹是封闭的“语境室”。跨领域连接仅允许通过显式 `[[链接]]` 触发。*参见：语境切换损耗、情境依赖检索*

---
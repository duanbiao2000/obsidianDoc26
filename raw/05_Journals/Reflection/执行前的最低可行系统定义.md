# 执行前的最低可行系统定义
将设计投入使用的触发条件从“所有模块就绪”改为“核心闭环可运行”。具体规则：对任一系统，定义其最小可执行闭环（例如口语训练：录音 → 自评 → 纠正 → 复录 一轮），完成闭环即算系统可用，所有后续优化必须在该闭环运行满 5 次后才允许进行。该规则已在 Obsidian 模板中固化，作为新建项目时的强制前置检查项。对比实验：对新建立的“每日反思”系统，采用最小闭环策略后，首周执行 7 次，系统修改仅 1 次，而旧有“知识图谱”系统同期仍停滞在结构设计中。